"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.Database = exports.noopTransformer = void 0;
const lsif_protocol_1 = require("@vscode/lsif-protocol");
const files_1 = require("./files");
exports.noopTransformer = {
    toDatabase: uri => uri,
    fromDatabase: uri => uri
};
class Database {
    fileSystem;
    uriTransformer;
    constructor() {
    }
    initialize(transformerFactory) {
        const workspaceRoot = this.getWorkspaceRoot().toString(true);
        this.uriTransformer = transformerFactory ? transformerFactory(workspaceRoot) : exports.noopTransformer;
        this.fileSystem = new files_1.FileSystem(workspaceRoot, this.getDocumentInfos());
    }
    stat(uri) {
        const transformed = this.uriTransformer.toDatabase(uri);
        const result = this.fileSystem.stat(transformed);
        if (result !== null) {
            return result;
        }
        const id = this.findFile(transformed);
        if (id === undefined) {
            return null;
        }
        return files_1.FileStat.createFile();
    }
    readDirectory(uri) {
        return this.fileSystem.readDirectory(this.uriTransformer.toDatabase(uri));
    }
    readFileContent(uri) {
        const transformed = this.uriTransformer.toDatabase(uri);
        let info = this.fileSystem.getFileInfo(transformed);
        if (info === undefined) {
            info = this.findFile(transformed);
        }
        if (info === undefined) {
            return null;
        }
        const result = this.fileContent(info);
        if (result === undefined) {
            return null;
        }
        return result;
    }
    asDocumentSymbol(range) {
        const tag = range.tag;
        if (tag === undefined || !(tag.type === 'declaration' || tag.type === 'definition')) {
            return undefined;
        }
        return lsif_protocol_1.types.DocumentSymbol.create(tag.text, tag.detail || '', tag.kind, tag.fullRange, this.asRange(range));
    }
    asRange(value) {
        return {
            start: {
                line: value.start.line,
                character: value.start.character
            },
            end: {
                line: value.end.line,
                character: value.end.character
            }
        };
    }
    toDatabase(uri) {
        return this.uriTransformer.toDatabase(uri);
    }
    fromDatabase(uri) {
        return this.uriTransformer.fromDatabase(uri);
    }
}
exports.Database = Database;
//# sourceMappingURL=database.js.map