import { URI } from 'vscode-uri';
import { Id, types } from '@vscode/lsif-protocol';
import { DocumentInfo } from './files';
import { Database, UriTransformer } from './database';
export declare class JsonStore extends Database {
    private version;
    private workspaceRoot;
    protected activeProject: Id | undefined;
    private vertices;
    private indices;
    private out;
    private in;
    constructor();
    load(file: string, transformerFactory?: (workspaceRoot: string) => UriTransformer): Promise<void>;
    getWorkspaceRoot(): URI;
    close(): void;
    private processVertex;
    private doProcessDocument;
    private processEdge;
    private doProcessEdge;
    getDocumentInfos(): DocumentInfo[];
    protected findFile(uri: string): {
        id: Id;
        hash: string;
    } | undefined;
    protected fileContent(info: {
        id: Id;
        hash: string;
    }): string | undefined;
    foldingRanges(uri: string): types.FoldingRange[] | undefined;
    documentSymbols(uri: string): types.DocumentSymbol[] | undefined;
    private toDocumentSymbol;
    hover(uri: string, position: types.Position): types.Hover | undefined;
    declarations(uri: string, position: types.Position): types.Location | types.Location[] | undefined;
    definitions(uri: string, position: types.Position): types.Location | types.Location[] | undefined;
    private findTargets;
    references(uri: string, position: types.Position, context: types.ReferenceContext): types.Location[] | undefined;
    private getResultPath;
    private getMostSpecificMoniker;
    private findVerticesForMoniker;
    private resolveReferenceResult;
    private item;
    private addLocation;
    private findRangesFromPosition;
    private static containsPosition;
    /**
     * Test if `otherRange` is in `range`. If the ranges are equal, will return true.
     */
    static containsRange(range: types.Range, otherRange: types.Range): boolean;
}
//# sourceMappingURL=jsonStore.d.ts.map