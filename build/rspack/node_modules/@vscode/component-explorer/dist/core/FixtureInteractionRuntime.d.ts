import { DebugPanelActivation, IDisposable } from './fixtureApi.js';
export declare const OUTPUT_DEBUG_PANEL_ID = "$output";
export interface ActiveDebugPanelSession extends IDisposable {
    sendAction(action: unknown): void;
}
type InputListener = (value: unknown, input: unknown) => void;
export declare class FixtureInputRuntime {
    private _input;
    private readonly _listeners;
    constructor(_input: unknown);
    watch(key: string, listener: InputListener): IDisposable;
    update(input: unknown): boolean;
}
export declare class FixtureDebugRuntime implements IDisposable {
    private readonly _activations;
    private readonly _activeSessions;
    private _output;
    private _hasOutput;
    connect(panelId: string, activate: DebugPanelActivation<unknown, unknown>): IDisposable;
    setOutput(output: unknown): void;
    get hasOutput(): boolean;
    get outputEditable(): boolean;
    getOutputSnapshot(): unknown;
    activate(panelId: string, publish: (state: unknown) => void): ActiveDebugPanelSession;
    dispose(): void;
    private _activateOutput;
}
export {};
//# sourceMappingURL=FixtureInteractionRuntime.d.ts.map