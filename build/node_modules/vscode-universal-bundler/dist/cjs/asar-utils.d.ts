export declare enum AsarMode {
    NO_ASAR = 0,
    HAS_ASAR = 1
}
export type MergeASARsOptions = {
    x64AsarPath: string;
    arm64AsarPath: string;
    outputAsarPath: string;
    singleArchFiles?: string;
    filesToSkipComparison?: (file: string) => boolean;
};
export declare const detectAsarMode: (appPath: string, asarPath?: string) => Promise<AsarMode>;
export declare const generateAsarIntegrity: (asarPath: string) => {
    algorithm: "SHA256";
    hash: string;
};
export declare function isUniversalMacho(file: string): Promise<boolean>;
export declare const mergeASARs: ({ x64AsarPath, arm64AsarPath, outputAsarPath, singleArchFiles, filesToSkipComparison, }: MergeASARsOptions) => Promise<void>;
