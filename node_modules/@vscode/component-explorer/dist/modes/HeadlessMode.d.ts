import { DisposeReport, RenderReport } from '../core/ErrorInfo.js';
import { ExplorerRuntime, FixtureModules } from '../core/index.js';
import { HeadlessFlatFixture, IComponentExplorerGlobal } from '@vscode/component-explorer-common';
declare global {
    interface Window extends IComponentExplorerGlobal {
    }
}
export declare class HeadlessMode implements ExplorerRuntime {
    private readonly _containerElement;
    /** Random ID generated on construction — changes on full page reload. */
    readonly pageInstanceId: string;
    private _updateVersion;
    private _root;
    private _fixtureModules;
    private _loadError;
    private _loadPromise;
    private _currentRendering;
    private _currentContainer;
    constructor(_containerElement: HTMLElement, loadFixtures: Promise<FixtureModules>);
    get updateVersion(): number;
    updateFixtures(fixtureModules: FixtureModules): void;
    bumpVersion(): void;
    listFixtures(): Promise<HeadlessFlatFixture[]>;
    disposeCurrentFixture(): Promise<DisposeReport>;
    renderFixture(fixtureId: string, input?: unknown): Promise<RenderReport>;
    /**
     * Install console + window error/rejection listeners that push into `events`.
     * Returns a `stop` function that uninstalls them. Listeners do not need to be
     * uninstalled in a `finally` ordering — multiple captures may overlap; the
     * outer `console.*` is restored to whatever was the previous value (LIFO via
     * the closure over `originalError` etc.).
     */
    private _captureEvents;
    private _toError;
    private _buildTree;
    private _flattenFixtures;
    private _findFixtureNode;
    private _installGlobalApi;
}
//# sourceMappingURL=HeadlessMode.d.ts.map