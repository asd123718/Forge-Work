import { Command, Option } from 'clipanion';
import '../vscode-observables/observables/dist/observableInternal/index.js';
import { DisposableStore } from '../vscode-observables/observables/dist/disposables.js';
import '../vscode-observables/observables/dist/observableInternal/debugLocation.js';
import { autorun } from '../vscode-observables/observables/dist/observableInternal/reactions/createEffect.js';
import '../vscode-observables/observables/dist/observableInternal/observables/derived.js';
import '../vscode-observables/observables/dist/observableInternal/utils/utils.js';
import '../vscode-observables/observables/dist/observableInternal/observables/observableFromEvent.js';
import { PlaywrightBrowserPageFactory } from '../browserPage.js';
import { BrowserComponentExplorer } from '../componentExplorer.js';
import { ConsoleLogger, verbosityToLogLevel, createLogFileOptions, logBuildInfo } from '../logger.js';
import { FileSystemStorage } from '../storage.js';
import { ResolvedConfig } from '../config.js';
import { AbsolutePath } from '../path.js';
import { ExplorerSession } from '../explorerSession.js';
import { compareScreenshots, ScreenshotCache, contentHash } from '../screenshotCache.js';
import { parseInputJson } from './inputArg.js';

class WatchCommand extends Command {
    static paths = [['watch']];
    static usage = Command.Usage({
        description: 'Watch for changes and compare screenshots against baseline',
        examples: [
            ['Watch all fixtures', '$0 watch'],
            ['Watch with config', '$0 watch -p component-explorer-watch-config.json'],
        ],
    });
    verbose = Option.Counter('-v,--verbose', 0, { description: 'Increase log verbosity (-v debug, -vv trace)' });
    logFile = Option.String('--log-file', { description: 'Append logs to this file (works with -v/-vv)' });
    project = Option.String('-p,--project', process.cwd(), { description: 'Project: a directory, vite config file, or component-explorer.json' });
    headed = Option.Boolean('--headed', false, { description: 'Show the browser window (useful for debugging)' });
    timeout = Option.String('--timeout', '30000', { description: 'Navigation timeout in milliseconds (default: 30000)' });
    input = Option.String('--input', { required: false, description: 'JSON object passed to every fixture as `RenderContext.input` (e.g. \'{"theme":"dark"}\'). Combine with `--input-<key> <value>` to override individual properties.' });
    async execute() {
        let input;
        try {
            input = parseInputJson(this.input);
        }
        catch (e) {
            this.context.stderr.write(`Error: ${e instanceof Error ? e.message : String(e)}\n`);
            return;
        }
        const disposables = new DisposableStore();
        const config = await ResolvedConfig.fromProjectPath(AbsolutePath.resolve(this.project), disposables);
        if (config.isFullConfig) {
            return this._executeWithConfig(config, disposables, input);
        }
        return this._executeLegacy(config, disposables, input);
    }
    async _executeLegacy(config, disposables, input) {
        const logger = new ConsoleLogger('watch', this.context.stdout, verbosityToLogLevel(this.verbose), createLogFileOptions(this.logFile, this.verbose));
        logBuildInfo(logger);
        const storage = new FileSystemStorage(config.screenshotDir);
        const browserFactory = new PlaywrightBrowserPageFactory(this.headed, Number(this.timeout));
        const serverConfig = config.sessions[0].server;
        logger.log('Starting Vite dev server...');
        const server = await serverConfig.createServer({ logger });
        logger.log(`Server running at ${server.url}`);
        const explorer = new BrowserComponentExplorer(browserFactory, server, logger);
        logger.log('Taking initial screenshots...');
        await captureAndCompare(explorer, storage, this.context.stdout, input);
        this.context.stdout.write('\nWatching for changes... (Ctrl+C to stop)\n\n');
        let stopped = false;
        process.on('SIGINT', () => { stopped = true; });
        while (!stopped) {
            try {
                await explorer.waitForUpdate();
                await captureAndCompare(explorer, storage, this.context.stdout, input);
            }
            catch (err) {
                if (stopped)
                    break;
                this.context.stderr.write(`[watch] Error: ${err}\n`);
            }
        }
        this.context.stdout.write('\nShutting down...\n');
        await explorer.dispose();
        await browserFactory.dispose();
        await server.dispose();
        disposables.dispose();
    }
    async _executeWithConfig(resolvedConfig, disposables, input) {
        const logger = new ConsoleLogger('watch', this.context.stdout, verbosityToLogLevel(this.verbose), createLogFileOptions(this.logFile, this.verbose));
        logBuildInfo(logger);
        const browserFactory = new PlaywrightBrowserPageFactory(this.headed, Number(this.timeout));
        logger.log(`Loaded config from ${resolvedConfig.configDir}`);
        const git = resolvedConfig.repo;
        const storage = new FileSystemStorage(resolvedConfig.screenshotDir);
        const sessions = new Map();
        const resolvers = new Map();
        const caches = new Map();
        try {
            // Set up sessions
            for (const sessionConfig of resolvedConfig.sessions) {
                await this._setupSession(sessionConfig, git, browserFactory, logger, sessions, resolvers, caches);
            }
            // Initial capture from all sessions
            for (const [name, session] of sessions) {
                logger.log(`Capturing initial screenshots: ${name}`);
                const screenshots = await captureAll(session.explorer, input);
                caches.get(name).replaceAll(screenshots);
                await writeScreenshotsToStorage(storage, name, screenshots);
            }
            // Print initial comparison
            if (resolvedConfig.compare) {
                const bl = caches.get(resolvedConfig.compare.baseline);
                const cr = caches.get(resolvedConfig.compare.current);
                printCompareResult(compareScreenshots(bl, cr), this.context.stdout, bl, cr);
            }
            this.context.stdout.write('\nWatching for changes... (Ctrl+C to stop)\n\n');
            let stopped = false;
            process.on('SIGINT', () => { stopped = true; });
            while (!stopped) {
                try {
                    const event = await raceUpdates(sessions, resolvers);
                    if (event.kind === 'hmr') {
                        logger.log(`HMR update in session: ${event.sessionName}`);
                        const cache = caches.get(event.sessionName);
                        const session = sessions.get(event.sessionName);
                        const screenshots = await captureAll(session.explorer, input);
                        cache.replaceAll(screenshots);
                        await writeScreenshotsToStorage(storage, event.sessionName, screenshots);
                    }
                    else if (event.kind === 'ref-change') {
                        logger.log(`Ref ${event.ref} moved to ${event.newCommit.toShort()} (no static worktree sessions to update)`);
                    }
                    // Print comparison
                    if (resolvedConfig.compare) {
                        const bl = caches.get(resolvedConfig.compare.baseline);
                        const cr = caches.get(resolvedConfig.compare.current);
                        printCompareResult(compareScreenshots(bl, cr), this.context.stdout, bl, cr);
                    }
                }
                catch (err) {
                    if (stopped)
                        break;
                    this.context.stderr.write(`[watch] Error: ${err}\n`);
                }
            }
        }
        finally {
            this.context.stdout.write('\nShutting down...\n');
            for (const session of sessions.values()) {
                await session.dispose();
            }
            for (const resolver of resolvers.values()) {
                resolver.dispose();
            }
            await browserFactory.dispose();
            disposables.dispose();
        }
    }
    async _setupSession(sessionConfig, _git, browserFactory, logger, sessions, _resolvers, caches) {
        logger.log(`Starting server: ${sessionConfig.name}`);
        const session = await ExplorerSession.create(sessionConfig.name, sessionConfig.server, browserFactory, { logger });
        sessions.set(sessionConfig.name, session);
        caches.set(sessionConfig.name, new ScreenshotCache());
        logger.log(`Server ready: ${sessionConfig.name} (${session.serverUrl})`);
    }
}
async function raceUpdates(sessions, resolvers) {
    const hmrPromises = [...sessions.entries()].map(([name, session]) => session.explorer.waitForUpdate().then(() => ({ kind: 'hmr', sessionName: name })));
    const refPromise = new Promise((resolve) => {
        if (resolvers.size === 0) {
            // Never resolves — no resolvers to watch
            return;
        }
        const previousCommits = new Map();
        for (const [name, resolver] of resolvers) {
            previousCommits.set(name, resolver.resolvedCommit.get());
        }
        const disposable = autorun(reader => {
            for (const [name, resolver] of resolvers) {
                const commit = resolver.resolvedCommit.read(reader);
                const prev = previousCommits.get(name);
                if (prev && !prev.equals(commit)) {
                    disposable.dispose();
                    resolve({ kind: 'ref-change', ref: resolver.ref, newCommit: commit });
                    return;
                }
            }
        });
    });
    return Promise.race([...hmrPromises, refPromise]);
}
// --- Capture helpers ---
async function captureAll(explorer, input) {
    const { fixtures } = await explorer.listFixtures();
    const screenshots = new Map();
    for (const fixture of fixtures) {
        const result = await explorer.screenshotFixture(fixture.fixtureId, { reloadOnError: true, input });
        if (result.image) {
            screenshots.set(fixture.fixtureId, result.image);
        }
    }
    return screenshots;
}
async function writeScreenshotsToStorage(storage, sessionName, screenshots) {
    for (const [fixtureId, png] of screenshots) {
        await storage.write(`${sessionName}/${fixtureId}.png`, png);
    }
}
function printCompareResult(result, stdout, baselineCache, currentCache) {
    const total = result.added.length + result.removed.length + result.changed.length + result.unchanged.length;
    const timestamp = new Date().toLocaleTimeString();
    stdout.write(`[${timestamp}] ${total} fixtures`);
    if (result.changed.length === 0 && result.added.length === 0 && result.removed.length === 0) {
        stdout.write(` — no changes\n`);
    }
    else {
        stdout.write(`\n`);
        for (const f of result.changed) {
            const bHash = baselineCache?.get(f) ? contentHash(baselineCache.get(f)) : '?';
            const cHash = currentCache?.get(f) ? contentHash(currentCache.get(f)) : '?';
            stdout.write(`  ~ ${f}  (baseline: ${bHash}, current: ${cHash})\n`);
        }
        for (const f of result.added) {
            stdout.write(`  + ${f}\n`);
        }
        for (const f of result.removed) {
            stdout.write(`  - ${f}\n`);
        }
    }
}
// --- Legacy helpers ---
async function captureAndCompare(explorer, storage, stdout, input) {
    const { fixtures } = await explorer.listFixtures();
    const added = [];
    const changed = [];
    const unchanged = [];
    for (const fixture of fixtures) {
        const result = await explorer.screenshotFixture(fixture.fixtureId, { reloadOnError: true, input });
        if (!result.image) {
            continue;
        }
        const currentPath = `current/${fixture.fixtureId}.png`;
        await storage.write(currentPath, result.image);
        const baselinePath = `baseline/${fixture.fixtureId}.png`;
        if (await storage.exists(baselinePath)) {
            const baseline = await storage.read(baselinePath);
            if (buffersEqual(result.image, baseline)) {
                unchanged.push(fixture.fixtureId);
            }
            else {
                changed.push(fixture.fixtureId);
            }
        }
        else {
            added.push(fixture.fixtureId);
        }
    }
    const timestamp = new Date().toLocaleTimeString();
    stdout.write(`[${timestamp}] ${fixtures.length} fixtures`);
    if (changed.length === 0 && added.length === 0) {
        stdout.write(` — no changes\n`);
    }
    else {
        stdout.write(`\n`);
        for (const f of changed) {
            stdout.write(`  ~ ${f}\n`);
        }
        for (const f of added) {
            stdout.write(`  + ${f}\n`);
        }
    }
}
function buffersEqual(a, b) {
    if (a.length !== b.length)
        return false;
    for (let i = 0; i < a.length; i++) {
        if (a[i] !== b[i])
            return false;
    }
    return true;
}

export { WatchCommand };
//# sourceMappingURL=watchCommand.js.map
