export declare enum LogLevel {
    silent = 0,
    info = 1,
    debug = 2,
    trace = 3
}
export interface ILogger {
    /** Alias for `info`. */
    log(message: string): void;
    info(message: string): void;
    debug(message: string): void;
    trace(message: string): void;
}
export interface LogFileOptions {
    /** Path to append log entries to. Multiple processes can safely append concurrently. */
    readonly path: string;
    /** Minimum log level to write to file (uses console level if not specified). */
    readonly level?: LogLevel;
}
export declare class ConsoleLogger implements ILogger {
    private readonly _prefix;
    private readonly _stream;
    private readonly _level;
    private readonly _logFileFd;
    private readonly _logFileLevel;
    constructor(_prefix: string, _stream: NodeJS.WritableStream, _level?: LogLevel, logFile?: LogFileOptions);
    log(message: string): void;
    info(message: string): void;
    debug(message: string): void;
    trace(message: string): void;
    private _writeToFile;
}
export declare const nullLogger: ILogger;
/** Map a `-v` counter (0, 1, 2, …) to a `LogLevel`. */
export declare function verbosityToLogLevel(verbosity: number): LogLevel;
export declare function logBuildInfo(logger: ILogger): void;
/** Create LogFileOptions from a file path and verbosity counter. */
export declare function createLogFileOptions(logFilePath: string | undefined, verbosity: number): LogFileOptions | undefined;
//# sourceMappingURL=logger.d.ts.map