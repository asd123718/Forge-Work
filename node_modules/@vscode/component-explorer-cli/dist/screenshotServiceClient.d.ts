export interface ScreenshotServiceClientOptions {
    readonly baseUrl: string;
    readonly apiKey?: string;
}
export interface CompareEntry {
    readonly fixtureId: string;
    readonly imageUrl: string;
    readonly changeCount?: number;
}
export interface CompareChangedEntry {
    readonly fixtureId: string;
    readonly beforeImageUrl: string;
    readonly afterImageUrl: string;
    readonly changeCount?: number;
}
export interface CompareResult {
    readonly baseCommitSha: string;
    readonly added: readonly CompareEntry[];
    readonly removed: readonly CompareEntry[];
    readonly changed: readonly CompareChangedEntry[];
    readonly unchanged: readonly CompareEntry[];
}
export declare class ScreenshotServiceClient {
    private readonly _baseUrl;
    private readonly _apiKey?;
    constructor(options: ScreenshotServiceClientOptions);
    compare(owner: string, repo: string, baseCommitSha: string, currentCommitSha: string, options?: {
        changeCountSinceDays?: number;
    }): Promise<CompareResult>;
}
//# sourceMappingURL=screenshotServiceClient.d.ts.map