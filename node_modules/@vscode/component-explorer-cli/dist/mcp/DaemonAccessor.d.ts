import type { ProxyApiClient } from '@hediet/simple-api/client';
import { type IObservable } from '@vscode/observables';
import type { DaemonApiType } from '../daemon/DaemonService.js';
export type DaemonClient = ProxyApiClient<DaemonApiType>;
/**
 * Provides daemon connections to the MCP server.
 *
 * Implementations own the full lifecycle: lazy startup, reconnection,
 * polling, staleness tracking. The MCP server only consumes this interface.
 */
export interface DaemonAccessor {
    /** Current daemon client, or `undefined` if not connected. */
    readonly connection: IObservable<DaemonClient | undefined>;
    /**
     * Request that a daemon be available. May lazily start or connect.
     * Returns `undefined` if no daemon could be obtained.
     */
    getDaemonOrStart(): Promise<DaemonClient | undefined>;
    /** Optional hint shown in error messages when no daemon is available. */
    readonly noDaemonHint?: string;
}
//# sourceMappingURL=DaemonAccessor.d.ts.map