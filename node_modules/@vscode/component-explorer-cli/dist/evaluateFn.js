const JS_ARG_SYMBOL = Symbol('jsArg');
function jsArg(expression) {
    return { [JS_ARG_SYMBOL]: true, expression };
}
function isJsArg(value) {
    return !!value && typeof value === 'object' && JS_ARG_SYMBOL in value;
}
function evaluateFn(target, fn, ...args) {
    const serializedArgs = args.map(a => isJsArg(a) ? a.expression : JSON.stringify(a));
    return target.evaluateJs(`(${fn.toString()})(${serializedArgs.join(', ')})`);
}
const explorerApi = jsArg('(() => { const e = window.__componentExplorer__; if (!e) throw new Error("Component Explorer not found"); return e; })()');
const explorerApiOrUndefined = jsArg('window.__componentExplorer__');
const explorerApiGetter = jsArg('(() => window.__componentExplorer__)');

export { evaluateFn, explorerApi, explorerApiGetter, explorerApiOrUndefined, jsArg };
//# sourceMappingURL=evaluateFn.js.map
