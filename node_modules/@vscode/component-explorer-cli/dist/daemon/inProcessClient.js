import { ProxyApiClient } from '../js-component-explorer/packages/simple-api/dist/chunk-WNXMRXWV.js';
import { AsyncStream } from '../js-component-explorer/packages/simple-api/dist/chunk-TTRCY65Z.js';
import 'zod';

/**
 * Creates a ProxyApiClient that calls a DaemonService's API in-process,
 * but JSON-round-trips all arguments and results to preserve the same
 * serialization boundary as the pipe transport.
 */
function createInProcessClient(api, context) {
    return new ProxyApiClient(async (method, arg) => {
        const serializedArg = JSON.parse(JSON.stringify(arg ?? {}));
        const result = await api.run(method, serializedArg, context);
        if (result instanceof AsyncStream) {
            const inner = result[Symbol.asyncIterator]();
            return new AsyncStream({
                async next() {
                    const r = await inner.next();
                    if (r.done) {
                        return r;
                    }
                    return { value: JSON.parse(JSON.stringify(r.value)), done: false };
                },
                async return() {
                    const r = await (inner.return?.() ?? Promise.resolve({ done: true, value: undefined }));
                    return r;
                },
            });
        }
        return JSON.parse(JSON.stringify(result));
    });
}

export { createInProcessClient };
//# sourceMappingURL=inProcessClient.js.map
