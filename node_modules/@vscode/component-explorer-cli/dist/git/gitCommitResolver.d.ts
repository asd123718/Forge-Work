import type { IDisposable } from '@vscode/observables';
import { type IObservable } from '@vscode/observables';
import { GitCommitId } from './gitCommitId.js';
import { AbsolutePath } from '../path.js';
export interface IGitCommitResolver extends IDisposable {
    readonly ref: string;
    readonly resolvedCommit: IObservable<GitCommitId>;
}
export declare class GitCommitResolver implements IGitCommitResolver {
    readonly ref: string;
    private readonly _gitRoot;
    private readonly _resolvedCommit;
    private readonly _watchers;
    private _debounceTimer;
    private _disposed;
    get resolvedCommit(): IObservable<GitCommitId>;
    private constructor();
    static create(gitRoot: AbsolutePath, ref: string): Promise<GitCommitResolver>;
    private _startWatching;
    private _scheduleResolve;
    private _resolve;
    dispose(): void;
}
//# sourceMappingURL=gitCommitResolver.d.ts.map