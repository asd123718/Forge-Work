import { type ChildProcess } from 'node:child_process';
export interface ProcessTree {
    readonly process: ChildProcess;
    /**
     * Terminates the process and its entire tree. Attempts a graceful shutdown
     * first and escalates to a forceful kill if the tree is still alive after a
     * grace period. Resolves once the tree has exited or has been force-killed.
     */
    kill(): Promise<void>;
}
export declare function spawnShellWithTree(cmd: string, options?: {
    cwd?: string;
}): ProcessTree;
//# sourceMappingURL=processTree.d.ts.map