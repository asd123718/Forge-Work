import { BrowserComponentExplorer } from './componentExplorer.js';

class ExplorerSession {
    name;
    _server;
    explorer;
    static async create(name, serverConfig, browserFactory, ctx) {
        const server = await serverConfig.createServer(ctx);
        const explorer = new BrowserComponentExplorer(browserFactory, server, ctx.logger);
        return new ExplorerSession(name, server, explorer);
    }
    constructor(name, _server, explorer) {
        this.name = name;
        this._server = _server;
        this.explorer = explorer;
    }
    get serverUrl() {
        return this._server.url;
    }
    get sourceTreeId() {
        return this.explorer.sourceTreeId;
    }
    async dispose() {
        await this.explorer.dispose();
        await this._server.dispose();
    }
}

export { ExplorerSession };
//# sourceMappingURL=explorerSession.js.map
