import { AbsolutePath } from './path.js';
export interface VisualCacheEntry {
    readonly expectedVisualDescriptions: readonly string[];
    readonly screenshotHash: string;
    readonly approvedAt: string;
    readonly comment: string;
}
export interface VisualCacheData {
    readonly version: 1;
    readonly fixtures: Record<string, VisualCacheEntry[]>;
}
export type VisualReviewStatus = 'no-expectations' | {
    expectedVisualDescriptions: readonly string[];
    status: 'approved' | 'needs-review';
    approval?: {
        approvedAt: string;
        comment: string;
    };
};
export declare class VisualCache {
    private readonly _filePath;
    private _data;
    constructor(_filePath: AbsolutePath);
    /**
     * Look up the review status for a fixture given its current descriptions and screenshot hash.
     */
    getReviewStatus(fixtureId: string, expectedVisualDescriptions: readonly string[], screenshotHash: string): VisualReviewStatus;
    /**
     * Approve a fixture's current screenshot. Adds an entry and evicts the oldest if over the limit.
     */
    approve(fixtureId: string, expectedVisualDescriptions: readonly string[], screenshotHash: string, comment: string): void;
    private _load;
    private _save;
}
//# sourceMappingURL=visualCache.d.ts.map